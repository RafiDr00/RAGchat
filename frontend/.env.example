# Backend URL — Next.js API routes proxy to this
# Development default: http://localhost:8001
BACKEND_URL=http://localhost:8001

# API key must match RAGCHAT_API_KEY in backend .env
# This is exposed to the browser — use a non-sensitive key or proxy auth server-side
NEXT_PUBLIC_RAGCHAT_API_KEY=your_api_key_here
